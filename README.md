# Baby Care AI 📱👶

매우 모던하고 사용자 친화적인 Flutter 기반 아기 성장 관리 앱입니다.

## 📋 프로젝트 개요

Baby Care AI는 부모들이 아기의 성장 과정을 체계적으로 기록하고 관리할 수 있도록 도와주는 크로스 플랫폼 모바일 애플리케이션입니다. Clean Architecture 원칙과 Material Design 3 가이드라인을 준수하여 개발되었습니다.

## ✨ 주요 기능

### 🍼 아기 관리
- **아기 등록**: 이름, 생년월일, 성별, 출생 체중/키, 메모 등 기본 정보 등록
- **아기 목록**: 등록된 아기들을 카드 형태로 직관적으로 표시
- **상세 정보**: 아기의 상세 정보와 성장 통계 확인
- **정보 수정**: 언제든지 아기 정보 수정 가능
- **상태 관리**: 활성/비활성 상태로 아기 정보 관리

### 🎨 사용자 경험
- **Material Design 3**: 최신 디자인 시스템 적용
- **다크 모드**: 시스템 설정에 따른 자동 테마 전환
- **반응형 디자인**: 다양한 화면 크기 지원
- **부드러운 애니메이션**: Hero 애니메이션과 페이지 전환 효과
- **직관적인 네비게이션**: GoRouter 기반 타입 안전 라우팅

### 🔧 기술적 특징
- **오프라인 지원**: 로컬 캐싱으로 네트워크 없이도 사용 가능
- **에러 처리**: 사용자 친화적인 에러 메시지와 복구 옵션
- **성능 최적화**: Shimmer 로딩과 이미지 캐싱
- **접근성**: 스크린 리더와 키보드 네비게이션 지원

## 🏗️ 아키텍처

### Clean Architecture 구조
```
lib/
├── clients/          # API 통신 계층
├── models/           # 데이터 모델
├── services/         # 비즈니스 로직 계층
├── states/           # 상태 관리 (Provider)
├── screens/          # UI 화면
├── widgets/          # 재사용 가능한 위젯
├── theme/            # 테마 및 디자인 시스템
├── utils/            # 유틸리티 함수
├── router.dart       # 라우팅 설정
└── main.dart         # 앱 엔트리 포인트
```

### 사용된 패키지

#### 핵심 패키지
- `flutter: sdk` - Flutter 프레임워크
- `provider: ^6.1.2` - 상태 관리
- `go_router: ^12.1.3` - 라우팅
- `dio: ^5.7.0` - HTTP 클라이언트

#### UI/UX 패키지
- `google_fonts: ^6.1.0` - Google Fonts 지원
- `cached_network_image: ^3.4.1` - 이미지 캐싱
- `shimmer: ^3.0.0` - 로딩 애니메이션
- `flutter_native_splash: ^2.3.13` - 스플래시 화면

#### 기능 패키지
- `flutter_dotenv: ^5.2.1` - 환경 변수 관리
- `shared_preferences: ^2.5.3` - 로컬 저장소
- `permission_handler: ^11.3.0` - 권한 관리
- `intl: ^0.19.0` - 국제화 지원
- `supabase_flutter: ^2.10.1` - 인증 (선택사항)

## 🚀 설치 및 실행

### 사전 요구사항
- Flutter SDK 3.9.2 이상
- Dart SDK 3.0.0 이상
- Android Studio / VS Code
- Android SDK (Android 개발용)
- Xcode (iOS 개발용, macOS만)

### 설치 방법

1. **저장소 클론**
   ```bash
   git clone <repository-url>
   cd babycareai
   ```

2. **의존성 설치**
   ```bash
   flutter pub get
   ```

3. **환경 변수 설정**
   `.env` 파일을 수정하여 API 설정을 구성하세요:
   ```env
   API_BASE_URL=http://localhost:8000
   SUPABASE_URL=your_supabase_url_here
   SUPABASE_ANON_KEY=your_supabase_anon_key_here
   ```

4. **앱 실행**
   ```bash
   # 개발 모드
   flutter run
   
   # 릴리스 모드
   flutter run --release
   ```

### 빌드

```bash
# Android APK
flutter build apk --release

# iOS (macOS만)
flutter build ios --release

# 웹
flutter build web --release
```

## 🔌 API 연동

### Baby Care API 엔드포인트

**Base URL**: `http://localhost:8000` (개발) 또는 실제 서버 URL

#### 아기 관리 API
- `POST /baby-care/babies` - 아기 생성
- `GET /baby-care/babies` - 아기 목록 조회
- `GET /baby-care/babies/{baby_id}` - 아기 정보 조회
- `PATCH /baby-care/babies/{baby_id}` - 아기 정보 수정
- `DELETE /baby-care/babies/{baby_id}` - 아기 정보 삭제

#### 인증
Supabase Auth Token을 Bearer Token으로 사용:
```dart
Authorization: Bearer <access_token>
```

### 데이터 모델

```json
{
  "id": 1,
  "userId": "user123",
  "name": "홍길동",
  "birthDate": "2024-01-15",
  "gender": "male",
  "birthWeight": 3.2,
  "birthHeight": 50.5,
  "notes": "첫째 아이",
  "isActive": true,
  "createdAt": "2024-01-20T10:30:00Z",
  "updatedAt": "2024-01-20T10:30:00Z"
}
```

## 🎯 주요 화면

### 1. 아기 목록 화면 (`BabyListScreen`)
- 등록된 아기들을 카드 형태로 표시
- Pull-to-refresh 기능
- 검색 및 필터링
- 활성/비활성 탭 구분
- FAB로 새 아기 등록

### 2. 아기 상세 화면 (`BabyDetailScreen`)
- 아기의 모든 정보 표시
- 프로필 이미지와 기본 정보
- 성장 통계 및 차트
- 수정/삭제 액션

### 3. 아기 등록/수정 화면 (`BabyFormScreen`)
- 직관적인 폼 인터페이스
- 실시간 유효성 검사
- 날짜 선택기
- 성별 선택 UI
- 자동 저장 기능

## 🎨 디자인 시스템

### Material Design 3
- **Color Scheme**: Seed color 기반 조화로운 색상 팔레트
- **Typography**: Noto Sans KR 폰트로 한글 최적화
- **Components**: 최신 Material 3 컴포넌트 사용
- **Elevation**: 적절한 그림자와 깊이감

### 테마 색상
- **Primary**: `#6750A4` (Material 3 Primary)
- **Baby Blue**: `#81C4E7` (남아 테마)
- **Baby Pink**: `#F8BBD9` (여아 테마)
- **Success**: `#006D3B`
- **Warning**: `#E65100`

## 📱 상태 관리

### Provider 패턴
- **BabyProvider**: 아기 데이터 상태 관리
- **BabyFormProvider**: 폼 상태 및 유효성 검사
- **로딩 상태**: 비동기 작업 상태 관리
- **에러 처리**: 사용자 친화적 에러 상태 관리

### 캐싱 전략
- **SharedPreferences**: 로컬 데이터 캐싱
- **5분 캐시 만료**: 적절한 데이터 신선도 유지
- **오프라인 지원**: 네트워크 없이도 캐시된 데이터 사용

## 🔧 개발 가이드

### 코딩 규칙
- **파일명**: snake_case 사용
- **클래스명**: PascalCase 사용
- **변수명**: camelCase 사용
- **상수명**: UPPER_SNAKE_CASE 사용

### 폴더 구조
- 기능별로 파일을 분리
- 재사용 가능한 위젯은 `widgets/` 폴더에
- 화면별 위젯은 `screens/` 폴더에
- 비즈니스 로직은 `services/` 폴더에

### 테스트
```bash
# 단위 테스트 실행
flutter test

# 위젯 테스트 실행
flutter test test/widget_test.dart

# 통합 테스트 실행
flutter drive --target=test_driver/app.dart
```

## 🚨 문제 해결

### 일반적인 문제들

1. **빌드 실패**
   ```bash
   flutter clean
   flutter pub get
   flutter build apk
   ```

2. **의존성 충돌**
   ```bash
   flutter pub deps
   flutter pub upgrade
   ```

3. **API 연결 문제**
   - `.env` 파일의 `API_BASE_URL` 확인
   - 네트워크 권한 확인
   - 서버 상태 확인

4. **권한 문제**
   - `android/app/src/main/AndroidManifest.xml` 확인
   - iOS의 경우 `ios/Runner/Info.plist` 확인

## 📈 성능 최적화

### 이미지 최적화
- `cached_network_image` 사용
- 적절한 이미지 크기 조정
- 메모리 캐시 관리

### 애니메이션 최적화
- `AnimationController` 적절한 dispose
- `SingleTickerProviderStateMixin` 사용
- 불필요한 리빌드 방지

### 메모리 관리
- `TextEditingController` dispose
- `FocusNode` dispose
- `ScrollController` dispose

## 🔐 보안

### 데이터 보호
- 민감한 정보는 환경 변수로 관리
- API 키는 `.env` 파일에 저장 (버전 관리 제외)
- 사용자 데이터 암호화

### 네트워크 보안
- HTTPS 통신 강제
- Certificate Pinning (프로덕션)
- 적절한 타임아웃 설정

## 🌍 국제화

### 지원 언어
- 한국어 (ko_KR) - 기본
- 영어 (en_US) - 예정

### 다국어 추가
1. `lib/l10n/` 폴더에 언어별 파일 추가
2. `MaterialApp`의 `supportedLocales`에 추가
3. 모든 사용자 대면 텍스트 다국어 처리

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다.

## 🤝 기여하기

1. Fork the Project
2. Create your Feature Branch (`git checkout -b feature/AmazingFeature`)
3. Commit your Changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the Branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📞 지원

문제가 있거나 질문이 있으시면 이슈를 등록해주세요.

---

**Baby Care AI** - 사랑스러운 아기의 성장을 함께 기록해보세요! 👶💕